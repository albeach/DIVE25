# Prevent npm from installing dependencies in subdirectories
prefix = false

# Force all installations to use the root package.json
save-prefix = false

# Prevent accidental global installations
global = false